/**
 * 開発時に切り替えるアプリ挙動のフラグ。
 */

/**
 * 1日に複数個のスタンプ獲得を許可するか。
 *
 * - true:  1日に何個でも獲得できる。カレンダーには日ごとに選んだ代表ロゴを表示する。
 * - false: 従来どおり1日1個まで（すでに獲得済みの日は保存しない）。
 *
 * 開発側でここを書き換えるだけで挙動を切り替えられる。
 */
export const ALLOW_MULTIPLE_STAMPS_PER_DAY = true;
